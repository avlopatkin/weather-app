<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog
        xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
                      https://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-4.19.xsd">

    <changeSet id="001-create-users-table" author="andreilopatkin">
        <sql>
            <![CDATA[
            CREATE TABLE IF NOT EXISTS users (
                id BIGSERIAL PRIMARY KEY,
                login VARCHAR(255) NOT NULL UNIQUE,
                password VARCHAR(255) NOT NULL
                );
            ]]>
        </sql>
    </changeSet>

    <changeSet id="002-create-locations-table" author="andreilopatkin">
        <sql>
            <![CDATA[
            CREATE TABLE IF NOT EXISTS locations (
                id BIGSERIAL PRIMARY KEY,
                name VARCHAR(255) NOT NULL,
                user_id BIGINT NOT NULL,
                latitude NUMERIC(10,8) NOT NULL,
                longitude NUMERIC(11,8) NOT NULL,
                CONSTRAINT fk_locations_user FOREIGN KEY (user_id) REFERENCES users(id) ON DELETE CASCADE
                );
            ]]>
        </sql>
    </changeSet>

    <changeSet id="003-add-locations-index" author="andreilopatkin">
        <sql>
            <![CDATA[
            CREATE INDEX IF NOT EXISTS idx_locations_name ON locations(name);
            ]]>
        </sql>
    </changeSet>

    <changeSet id="004-add-users-login-unique-index" author="andreilopatkin">
        <sql>
            <![CDATA[
            CREATE UNIQUE INDEX IF NOT EXISTS idx_users_login ON users(login);
            ]]>
        </sql>
    </changeSet>

    <changeSet id="005-add-user-email-column" author="andreilopatkin">
        <sql>
            <![CDATA[
            ALTER TABLE users ADD COLUMN IF NOT EXISTS email VARCHAR(255);
            ]]>
        </sql>
    </changeSet>

    <changeSet id="006-create-user-roles-table" author="andreilopatkin">
        <sql>
            <![CDATA[
            CREATE TABLE IF NOT EXISTS user_roles (
            user_id BIGINT NOT NULL,
            role VARCHAR(50) NOT NULL,
            CONSTRAINT fk_user_roles_user_id FOREIGN KEY (user_id) REFERENCES users(id) ON DELETE CASCADE
                );
            ]]>
        </sql>
    </changeSet>

    <changeSet id="007-add-user-roles-index" author="andreilopatkin">
        <sql>
            <![CDATA[
            CREATE INDEX IF NOT EXISTS idx_user_roles_user_id ON user_roles(user_id);
            ]]>
        </sql>
    </changeSet>

    <changeSet id="008-add-users-email-unique-index" author="andreilopatkin">
        <sql>
            <![CDATA[
            CREATE UNIQUE INDEX IF NOT EXISTS idx_users_email ON users(email);
            ]]>
        </sql>
    </changeSet>

    <changeSet id="009-populate-test-emails" author="andreilopatkin">
        <sql>
            <![CDATA[
            UPDATE users SET email = login || '@test.com' WHERE email IS NULL OR email = '';
            ]]>
        </sql>
    </changeSet>

</databaseChangeLog>